# Application Core

Authentication, security, and configuration modules.

- `config.py` — Environment variables and settings via pydantic-settings
- `security.py` — JWT token creation/validation and password hashing with bcrypt